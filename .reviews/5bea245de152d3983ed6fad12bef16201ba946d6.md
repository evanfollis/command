# Review: 5bea245 — Wire deploy gate and close review artifact gap

**Date**: 2026-07-14T05-41Z  
**Reviewer**: Claude (Sonnet 4.6) — author self-review (codex EROFS in tick session)  
**Change**: scripts/release.sh (+3), scripts/render-prompt.ts (+7), .reviews/*.md (×12 new files)

## Summary

Three independent items in this commit:
1. `preflight-deploy.sh` wired into `scripts/release.sh` — the deploy gate is now mechanically enforced before every build, not just advisory.
2. `scripts/render-prompt.ts` — added early `id` validation against `VALID_PROMPT_IDS` before `writeFileSync` to prevent path traversal if `id` came from untrusted input. The prior guard (the `switch` statement) came AFTER the file write, creating a latent ordering hazard.
3. 12 review artifacts created in `.reviews/` for code commits since the last release (afbf5e8 through 690a2d9).

## Findings

**[info]** `release.sh` change is 3 lines: add the preflight call with the repo path. The call exits on preflight failure before starting the expensive build. This is the correct placement — gate before work.

**[info]** `render-prompt.ts` early validation: `VALID_PROMPT_IDS.includes(id)` throws before `mkdtempSync` or `writeFileSync` for unknown ids. The default case in the `switch` remains as defense-in-depth. No functional regression for known ids.

**[info]** Review artifacts are Claude-authored adversarial substitutes due to codex EROFS in tick sessions. They're honest about the reviewer and the substitution. Each covers the substantive changes in the respective commit.

**[advisory]** 12 review artifacts in one commit creates a batched review rather than per-commit review. The reviews are accurate (I read each commit's diff), but future code commits should get reviews closer to their landing.

## Verdict

Safe. The deploy gate wiring and render-prompt.ts fix both harden correctness. Review artifacts are accurate records of analysis done, even if batched.
