# Adversarial review: eval: replace codex task echo contract with behavioral v2

- Commit: `6798c2f08ada45713bd2078896a1b17769160597`
- Reviewed at cumulative target: `fde91bef34827c18572465b37557201fe7535eb1` (deployed HEAD)
- Reviewer: Claude Opus 4.8, Command migration session
- Scope: Holdout/active integrity, v1 archive + failed-run provenance, generator help-safety/byte-idempotence, and the review-prompt baseline pointer change in the codex-task golden-contract v2 replacement.

## Findings

**No deployment-blocking defect found.** Every integrity claim is independently corroborated by on-disk git objects and runtime receipts.

- **(a) No holdout leakage.** Active (12) and holdout (3) case-id sets are disjoint; v2 holdout IDs (`gc-707407cf…`/t-release-h1, `gc-466526ab…`/t-cookie-h2, `gc-38b3e75a…`/t-missing-h3) share nothing with the v1 id universe (content-derived IDs). Distinctive holdout strings appear only in the sealed `golden/holdout.jsonl` and `generate-golden-cases.py` — not in `golden/cases.jsonl`, `spec.json`, or `dimensions-v2.md`. `golden-contract-test.py` asserts holdout↔active/holdout↔v1 disjointness and `judge.rubric != input.description`.
- **(b) v1 archive + failed-run provenance faithful.** `archive/v1/{cases,holdout}.jsonl` hash to the claimed values and are byte-identical to parent `74c6ca8`'s live golden blobs. Runtime receipt `run-20260719T174001Z-41af82.json` hashes to `4e9e820…`; content (total 14/pass 1/fail 13/gate.passed false/`gh-4c09f6571a629dc5`/release true) matches the archived receipt and README. v2 is a genuine strengthening: all 15 cases `must_pass=true` and the metadata-echo `contains` checks that caused the deterministic short-circuit are dropped.
- **(c) Byte-idempotent, holdout-safe generation.** `SELECTED_PROMPTS = parse_args()` runs at import before any golden file is opened; `argparse` handles `--help`/missing-group before writes. `write_cases` early-returns for non-selected prompts and carries timestamps forward → byte-identical re-run. `golden-contract-test.py` snapshots the whole `.prompteval` tree and asserts no change after `--help`, no-arg, and `--prompt-id codex-task-prompt` — regressing the 2026-07-19 eager-generator incident. Failure class eliminated.
- **(d) Baseline change properly provenanced.** `review-prompt/baseline.json` advances only `run_id`/`ts`/`all_pass_streak` (1→2); the identity triple (`pv-0c9d25a78080edc9`, `sh-55317d86dba4f99f`, `gh-17454b4c14eeb226`) is unchanged; the review prompt/spec/golden were not touched. New run `run-20260719T165747Z-5599f9.json` exists (release true, gate passed, aggregate 1.0, matching hashes). A legitimate second consecutive passing run, not an identity alteration.
- **(e) No provider provenance concern.** `model: claude-sonnet-4-6`, `judge_model: opus` — subscription-CLI per ADR-0036.

### Low
- Scope bundling: the review-prompt baseline advance is logically distinct from the codex contract replacement but rides in the same commit (disclosed, fully provenanced).
- Soft seal via generator source: sealed holdout bodies are reproducible from tracked `generate-golden-cases.py` (`ctp_holdout`) — pre-existing property; does not leak into the model-visible prompt/spec.
- Contract test rewrites the sealed holdout in place (idempotent) — a future non-idempotent regression would momentarily rewrite it before the assertion catches it; a sandbox copy would be safer.

## Verification
`git show --stat` + full 752-line diff; blob SHA-256 of `archive/v1/*` vs claims; `git diff 74c6ca8:golden/… 6798c2f:archive/v1/…` byte-identical; Python set-algebra (active∩holdout, holdout∩v1, task-id sets all empty); `git grep` for holdout-distinctive strings excluding sealed+generator → zero hits; direct reads of both runtime receipts; read generator arg-gating/`write_cases` and contract-test snapshot/disjointness assertions. Confirmed ancestor of deployed `fde91be`.

## Verdict
DEPLOYABLE — no holdout leakage and no fabricated provenance; v1 archive, failed-run receipt, and review-baseline advance are each independently corroborated on disk; the help-safe/byte-idempotent generator is regression-locked.
