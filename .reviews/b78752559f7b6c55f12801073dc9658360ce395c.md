# Adversarial review: Strengthen Codex task durability claims

- Commit: `b78752559f7b6c55f12801073dc9658360ce395c`
- Reviewed at cumulative target: `fde91bef34827c18572465b37557201fe7535eb1` (deployed HEAD)
- Reviewer: Claude Opus 4.8, Command migration session
- Scope: One eval-iteration commit for `codex-task-prompt` — golden `cases.jsonl` rubric loosening, eec522 failed-run archive, prompt durability line, and locking test assertions.

## Findings

**No deployment-blocking defect found.** A faithful, seal-preserving eval-feedback increment that honestly leaves the gate red.

### Low
- **(a) `cases.jsonl` churn is not a reorder and does not risk holdout overlap.** The 24 changed lines are the same 12 active cases in the same order, each edited two ways: the `goal-not-addressed` rubric gains one sentence blessing a truthful "no output + exact command/cwd" read-only handoff, and `last_validated` bumps. That is a genuine but defensible semantic loosening — it resolves a real contradiction where the old rubric false-failed the exact honest behavior the co-resident `fabricated-execution-evidence` rubric rewards (which failed active `gc-77322229…` in eec522). Scoped to "when the probe lacks execution authority," so no broad evasion loophole; mirrored in the generator's `GOAL_RESPONSIVENESS`. Active/holdout IDs disjoint at both the commit and HEAD.
- **Rubric divergence between active and sealed (noted, not blocking).** The loosened rubric was applied to active but not the sealed holdout (correct seal-preserving move) — the sealed set temporarily measures a stricter criterion for the same failure mode. Direction is conservative (risks holdout false-fails, never inflated pass rates); resolved later via legitimate rotate/burn (`1006ea9`, `6c15d8e`).
- **(b) eec522 receipt is faithful provenance, verified against the live runtime file.** `sha256sum` of `run-20260719T195258Z-eec522.json` = `a48305c0…`, matching the committed receipt. Every field confirmed (`release:true`, `cached_allowed:false`, `gate.passed:false`, `aggregate:0.8`, `gh-6331fa33a0a0a68c`, `sh-33de56c408b2aec7`, `pv-54209cf85ead96c6`, `judge_unknown_ratio 0.0222`). The three not-passed cases match `active_failed`+`sealed_unknown` exactly; aggregate (12/15) and unknown ratio (1/45) internally consistent. Pre-correction holdout sha `e7ad1dd7…` matches.
- **Sealed-ID disclosure (informational).** Archive discloses sealed ID `gc-38b3e75ad2a0ab4d` and that its judge output was unparseable — run-level metadata already visible to anyone who ran the release, not sealed input/output/rubric bodies; that case was subsequently rotated out.
- **(c) Prompt one-liner is substantive.** The added durability bullet (write ordering, partial-failure recovery, idempotent replay/backfill, authoritative producer ownership before "exact/comprehensive"; "never mix comprehensive claims with acknowledged atomicity gaps") maps onto the demonstrated failure of `gc-dacb6094…` and mirrors the workspace S1-P3 rule. Fixes the model where it failed, not the rubric.
- **(d) New test assertions lock real behaviors.** `codex-task-builder-test.ts` asserts the rendered builder output contains the new durability sentences; `golden-contract-test.py` ties generator↔materialized rubric consistency, prompt-source phrases, and anchors eec522 on the live runtime digest. The one soft spot: `eec522_receipt['sha256'] == EEC522_RUN_SHA` is tautological on a host without the runtime file (same `if X.exists()` pattern as every run).
- **(e) No eval-evidence integrity violation.** No accepted baseline exists for `codex-task-prompt`, so none altered; sealed holdout untouched/byte-identical (`e7ad1dd7…` before/after); prompt edit is the commit's stated purpose. CURRENT_STATE honestly records "failed closed at 0.8" and "Deployment remains blocked."

## Verification
Confirmed ancestor of deployed `fde91be`. Full diff + current `codex-task-prompt.md` + `golden-contract-test.py`. `sha256sum` on the live eec522 runtime file → matches receipt; parsed and cross-checked every field, not-passed set, aggregate/unknown-ratio math. Extracted active+holdout IDs at the commit and HEAD (IDs only) — disjoint at both; pre-correction holdout sha matches. Did not execute `golden-contract-test.py` (it invokes the writing generator) to honor read-only; verified assertions by inspection against runtime evidence.

## Verdict
DEPLOYABLE — faithful failed-run provenance (digest-verified), a legitimate rubric correction plus substantive prompt hardening, sealed holdout untouched and active/holdout IDs disjoint, honest gate-red bookkeeping; no seal violation, fabrication, or false pass claim.
