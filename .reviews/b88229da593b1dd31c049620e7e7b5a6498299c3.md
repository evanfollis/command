# Adversarial review: eval: make offline auth synthesis evidence-bound

- Commit: `b88229da593b1dd31c049620e7e7b5a6498299c3`
- Reviewed at cumulative target: `fde91bef34827c18572465b37557201fe7535eb1` (deployed HEAD)
- Reviewer: Claude Opus 4.8, Command migration session
- Scope: Retirement of a forced-proxy-causality golden case, its archive provenance, the reworded evidence-bound prompt, the added contract test, and the new thread-opening-frame baseline.

## Findings

No deployment-blocking defect found. All provenance verifies byte-exactly and the correction is genuine, not relocated.

- **(a) Prompt reword genuinely evidence-binds — it does not move the causal claim.** Old text asserted "look for the server-generated URL root class" and weakened the origin rule with "*without a canonical trusted origin policy*." New `offline-synthesis-prompt.md:9` reframes it as a hypothesis to test against supplied evidence, makes "**never** derive public origins from request headers" unconditional, and adds "Do not force a reverse-proxy explanation from a generic authentication symptom; name the missing discriminating evidence instead." The paired golden rubric (`gc-aa4f4c1f16eb0b30`, `unsupported-auth-causality`) FAILs unsupported proxy/WebKit/password-manager causality and mechanism-specific fixes (UA-keyed dedup, origin rewriting) and PASSes observation/hypothesis separation plus a bounded discriminating path.
- **(b) Retired-case provenance faithful.** Archived `gc-3b64928414ae568a.jsonl` hashes to `92f536b0…` (matches README+receipt) and is byte-identical to the removed golden line. Runtime `run-20260719T181501Z-caf280.json` hashes to `1761d5c9…`; cache `ck-07d0912cf2acee17.json` to `4509b3eb…` — both match. Genuine uncached release evidence (`release:true, cached_allowed:false`), `gate.passed:false`, sole failing case exactly `gc-3b64928414ae568a`.
- **(c) thread-opening-frame/baseline.json does not contaminate an accepted baseline.** New file (first git appearance here), a faithful record of real run `run-20260719T181436Z-17a5e0` (all 16 per-case values, release true, gate passed, aggregate 0.875). Replaces no prior baseline.
- **(d) No holdout leakage / eval-integrity issue.** No `holdout.jsonl` touched. Contract test reads holdout only via `sha256` byte-digest, asserts unchanged after regeneration. `offline-synthesis-prompt` correctly left fail-closed with no baseline at this commit (golden changed); fresh baseline accepted later (HEAD `gh-962ef3e74c497e41`, aggregate 1.0).
- **(e) Aligns with the reverse-proxy origin rule, and the corrected golden is itself evidence-true.** `middleware.ts:26` pins `COMMAND_ORIGIN`; `api/auth/route.ts` uses relative `Location`; `check-patterns.ts` forbids `req.url`/`req.headers`-derived origins. The new golden's embedded facts are true, not unsupported — the correction does not reintroduce the failure class it retires.

### Low
- **New contract test not wired into any automated gate.** `offline-synthesis-contract:test` is invoked by nothing in the deploy path (`check`/`build`/`smoke`/`release.sh`/`release:test`/`preflight-deploy.sh`; no CI). Defense-in-depth gap, not a hole — the fail-closed accepted-baseline preflight still blocks shipping a changed prompt without a fresh baseline. (Addressed by the migration wiring contract tests into `make check`.)
- Run/cache provenance assertions are environment-guarded (`if RUN.exists()`) against runtime files outside the immutable release; only the in-repo archived-bytes SHA is enforced unconditionally.
- Scope nit: the thread-opening-frame baseline is unrelated to the offline-synthesis correction yet rides in this commit; "preserve the accepted thread baseline" is imprecise for a first-time record. No integrity impact.

## Verification
Full diff; current `offline-synthesis-prompt.md` and `offline-synthesis-contract-test.py`. Recomputed and matched all four provenance SHAs (archived case, run, cache); archived case byte-identical to removed golden line. Inspected runtime run/cache JSON. Confirmed `offline-synthesis-prompt` fail-closed at b88 and new-golden-consistent baseline at HEAD. Cross-checked thread baseline against its runtime run. Confirmed no `holdout.jsonl` in the diff; holdout IDs surface only as ID+boolean. Verified `COMMAND_ORIGIN` pinning, relative redirects, and the `check-patterns.ts` guard in current code.

## Verdict
DEPLOYABLE — provenance is byte-faithful, the prompt/golden correction genuinely enforces evidence-bound causality and aligns with the pinned-origin invariant, no holdout or accepted baseline was contaminated; the only gaps are non-blocking defense-in-depth notes.
