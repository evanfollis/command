# Adversarial Review: browser-smoke scripts

**Reviewer**: Claude agent (Codex unavailable — EROFS)
**Reviewed**: scripts/browser-smoke.ts, scripts/browser-smoke-wrapper.sh, scripts/browser-libs-setup.sh
**Date**: 2026-05-01

## Findings and resolutions

**F1 — WS evidence gap (ship-blocking)**: Original script only checked static HTML on attach pages. Fixed: added `waitForFunction` that polls until `<pre>` has content, then asserts `pane chars > 0`. Verified: general=8101 chars, general-codex=12933 chars.

**F2 — No JS error capture (ship-blocking)**: Added `context.on('pageerror')` handler that increments counter and logs. Final check asserts `errors=0`. Verified: 0 unhandled errors across all 7 routes.

**F3 — Auth throw path swallowed check (should fix)**: Wrapped `Promise.all` in try/catch. On auth failure, check() is called with `false`, screenshot captured, remaining checks skipped with clear message.

**F4 — Missing chromium binary check (should fix)**: Added `find node_modules/playwright-core/.local-browsers -name "chrome-headless-shell"` check in wrapper before setting env vars. Clear error message with recovery command.

**F5 — Selector looseness (informational)**: Session card selectors use broad text match. Accepted tradeoff — internal single-user tool; structural change would break this naturally rather than pass silently.

**F6 — Unused `existsSync` import**: Removed.

**F7 — `apt-get` failure masked (informational)**: `DEB_COUNT -eq 0` guard is the real safety net. `|| true` masking of `grep -v "^W:"` pipeline acceptable since apt errors don't start with W.

## Final run output

```
✓ /login renders password field — count=1
✓ login form → redirects to /
✓ / has session cards — count=6
✓ portfolio card expands and shows content — prose/pre count=2
✓ /attach/general renders "Attached to" h1
✓ /attach/general has "Live pane" section
✓ /attach/general pane has WS snapshot content — pane chars=8101
✓ /attach/general-codex renders "Attached to" h1
✓ /attach/general-codex has "Live pane" section
✓ /attach/general-codex pane has WS snapshot content — pane chars=12933
✓ /artifacts renders h1 "Artifacts"
✓ /artifacts shows source labels — count=38
✓ no unhandled browser JS errors — errors=0

BROWSER SMOKE PASSED
```

## Accepted tradeoffs

- `--no-sandbox`: mandatory for root headless. Correct.
- Fixed `waitForTimeout` sleeps: appropriate for interactive page transitions on a controlled host.
- `/tmp` ephemeral libs: clean documented failure path via wrapper check.
- Login double-submit: Playwright headless can't reach autofill+Enter race. Intentional gap.

## Verdict: SHIP
