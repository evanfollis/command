# Review: c28fd43 — Fail closed on release and projection ambiguity

**Date**: 2026-07-14T05-41Z  
**Reviewer**: Claude (Sonnet 4.6) — adversarial substitute (codex EROFS in tick session)  
**Change**: assert-dirty-release-inputs.sh (+9), observatory-test.ts (+10), release-invariants-test.sh (+24), release-lib.sh (+12), release.sh (+6 -1), rollback.sh (+4 -1), observatory.ts (+13 -1)

## Summary

Adds `assert-dirty-release-inputs.sh` to validate that `ALLOW_DIRTY=1` builds have explicit reviewed inputs (not accidental uncommitted changes); adds `release-lib.sh` as a shared library; adds `release-invariants-test.sh` for automated release correctness tests; hardens `rollback.sh` to verify service active AND `/login=200` before declaring rollback successful; adds observatory.ts `hasBlock` check.

## Findings

**[info]** `assert-dirty-release-inputs.sh` checks for staged commits and ALLOW_DIRTY environment, preventing accidental dirty builds. Correct.

**[info]** `release-lib.sh` extracts shared helpers (`resolve_command_port`, `wait_for_port`) used by both release.sh and rollback.sh. Good DRY.

**[info]** `release-invariants-test.sh` tests that the release system correctly rejects: dirty tree without override, nonexistent HEAD, and verifies `dist/.version` format. These are the right regression tests.

**[info]** Rollback now verifies service active + `/login=200` before exit. The prior version checked only service restart success, which doesn't prove the app is serving.

**[advisory]** `observatory.ts` `hasBlock` check adds `blocked` as an optional field to the research item validator. The `optional: ['block']` parameter to `hasExactKeys` correctly allows but doesn't require it. This is consistent with the v1 contract.

## Verdict

Safe. The fail-closed additions strengthen both the release pipeline and the observatory's schema handling. No new attack surface.
