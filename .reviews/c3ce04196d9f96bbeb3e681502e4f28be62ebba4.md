# Adversarial Review: c3ce041 — Tighten wording on URL-from-req.url ban

**Reviewer**: Claude Sonnet 4.6 (adversarial substitute — Codex EROFS blocked in tick sessions)
**Date**: 2026-07-14
**Scope**: CLAUDE.md wording update + check-patterns.ts description tightening + 5bea245 review artifact

---

## Changes reviewed

1. `CLAUDE.md`: "never build absolute URLs from … or `new URL(path, req.url)`" → "never … by passing `req.url` as the base to a `URL()` constructor"
2. `scripts/check-patterns.ts`: comment updated to match revised wording
3. `.reviews/5bea245de152d3983ed6fad12bef16201ba946d6.md`: new review artifact for prior commit

---

## Findings

**No real risks in this change.** The diff is documentation-only plus a new review artifact file.

**Wording correctness**: The new wording ("passing `req.url` as the base to a `URL()` constructor") is more precise than the old ("or `new URL(path, req.url)`") because it covers all `new URL(anything, req.url)` forms, not just the one illustrative example. The scanner at `check-patterns.ts` already matched both forms (the regex is broader than any one phrasing). The wording change makes the docs match the scanner's actual scope.

**Pattern coverage**: The scanner covers `new URL([^,]+, req\.url` and `NextResponse.redirect(new URL(`. No gap between the documented description and the implemented check.

**No behavior change**: Confirmed — the regex patterns in check-patterns.ts did not change; only the comment text above them changed.

**Review artifact fidelity**: `.reviews/5bea245.md` covers the 5bea245 commit (deploy-gate wiring). Review is internally consistent with what that commit did (ordering hazard fix, review artifacts for prior commits, release.sh preflight wiring). No issues.

---

## Verdict

Ship. Pure documentation alignment, no logic change.
