# Adversarial review: prompt: require verified bounded Codex task closure

- Commit: `db026ba4274ea9a47bde48105d549a1fc19a9be3`
- Reviewed at cumulative target: `fde91bef34827c18572465b37557201fe7535eb1` (deployed HEAD)
- Reviewer: Claude Opus 4.8, Command migration session
- Scope: Prompt-eval iteration only — `codex-task-prompt.md` contract edit, archived failing-run 318ec6 provenance, judge alignment record, and builder/contract test coverage. No product web surface, route, or auth code touched.

## Findings

**No deployment-blocking defect found.** The four adversarial questions resolve in the commit's favor, with three graded observations.

### Low — Rubric edit is goalpost-adjacent but is legitimate rubric repair, not gaming
The one-line `golden/cases.jsonl` change rewrites the `debugging-without-source-grounding` rubric for active case `gc-466822e89b2392f2` — the case that failed run 318ec6. It survives scrutiny: the **old** rubric encoded a logically impossible PASS (required "a precise, source-compatible diff for the actual empty-array failure class," but an empty array is valid in `metaLearning.ts` — no such failure class exists), effectively forcing fabrication of a fix for a non-bug. The **new** rubric adds a distinct FAIL branch ("proposes a source-incompatible change") and requires explicit source inspection plus reconciliation before either a correction or a bounded "no change warranted + name missing evidence" conclusion. Stricter where it matters, removes an impossible demand. `must_pass` stays `true`. No baseline accepted off the failing run (`gate.passed=False`; `baseline.json` untouched) — the ADR-0039 gate correctly stays red; the commit iterates, it does not promote.

### Low — Contract-test deep provenance binding is host-guarded
`golden-contract-test.py` gates its strongest assertions behind `if V2_FEEDBACK_RUN.exists()`/`if path.exists()`. On a host without the root-only runtime dir (fresh clone/CI), only the committed receipt's self-consistency is checked. Full binding confirmed on this host; accepted portability tradeoff, but CI provenance binding is weaker than the producing host.

### Low — Builder test proves wiring/containment, not Codex behavioral compliance
`codex-task-builder-test.ts` asserts the rendered prompt contains the contract clauses and that the false-premise `{description}` lands after "Execution contract:" — proving the edit is on the real dispatch path (`buildCodexPrompt → fillTemplate(loadPrompt('codex-task-prompt'))`, `executor.ts:247`) and no un-substituted tokens remain. It does not prove a dispatched Codex agent obeys the clauses; behavioral proof correctly lives in the golden set.

## Verification
- Provenance byte-exact: `run-20260719T183312Z-318ec6.json` hashes to `91eaf1a6…` (matches receipt+test constant). All three cache files match the receipt values. Run internals: `release=True`, `cached_allowed=False`, `aggregate=0.8`, `gate.passed=False` naming exactly the three must-pass failures; 15 cases = 12 active + 3 holdout; math coherent. Not backfilled.
- No holdout leakage: holdout IDs appear 0 times in the diff; edited case is active; README states holdout contents not copied.
- Honest disagreement: `alignment.jsonl` stores the old rubric with `human_verdict:"pass"` and records the judge's fail/unknown/pass split and the human override — motivating the rubric repair.
- Eval integrity: contract test pins failed cache outputs to their real content; both new tests pass at HEAD; capture side-effect gated (`promptCapture.ts:18 if (process.env.PROMPTEVAL_RENDER) return`); no `baseline.json`/`holdout.jsonl` touched.

## Verdict
DEPLOYABLE — provenance verifies byte-exact, no holdout leakage, `must_pass` preserved, and the prompt-eval gate correctly stays red on the pending Codex baseline (fail-closed by design); the rubric edit is justified repair of an impossible PASS condition, not goalpost-moving.
