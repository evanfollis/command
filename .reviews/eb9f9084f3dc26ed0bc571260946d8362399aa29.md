# Adversarial review: Retire Command's remote-operation web surface

- Commit: `eb9f9084f3dc26ed0bc571260946d8362399aa29`
- Reviewed at cumulative target: `fde91bef34827c18572465b37557201fe7535eb1` (deployed HEAD)
- Reviewer: Claude Opus 4.8, Command migration session
- Scope: Verify the large operator-surface removal is complete, reachable-mutation-free, and mechanically enforced — not merely hidden — in the current tree (already an ancestor of deployed HEAD).

## Findings

### No deployment-blocking defect found.

No surviving reachable mutation or operator path exists on the authenticated web surface. Every removed route, its libs, and its runtime dependencies are gone, and the boundary is enforced by an executable gate wired into `npm run check`/`npm run build`, not by convention.

### Low — Empty leftover route directories in the working tree (cosmetic, not reachable)
`find` shows `src/app/operator-tools`, `src/app/attach`, `src/app/sessions`, `src/app/api/{send,sessions,review,threads,executive,client-report}` still present as **empty** directories (git deletes files, not now-empty dirs). `git ls-files` confirms zero tracked files under any of them; Next creates no route from a directory with no `page.tsx`/`route.ts`; `git archive`/clone omit empty dirs. No route, no reachability. Local clutter only.

### Low — `src/lib` retains mutation helpers; web isolation is guarded by string-scan, not import-graph proof
`src/lib/tmux.ts` still exports `sendKeys`/`sendNamedKeys`, called by `src/lib/{review,executive,executor}.ts` for the prompt-eval renderer/CLI — not imported by any web route. Two web routes import `tmux.ts` (`api/project-status/route.ts:6`, `api/context-usage/[name]/route.ts:5`) but only its read-only exports. The regression guard is the literal-string scan in `product-boundary-test.ts:87` (`\bsendKeys\b|\bsendNamedKeys\b`), a name-based caller-side scan rather than a module-boundary/import-graph assertion — a mutation helper renamed/re-exported under a different identifier would evade it. Adequate for the current surface; noted as the weakest link. (Addressed structurally by the migration's hard Plane-A/B boundary work.)

### Low — Dead `ws/` exclusion in middleware matcher
`src/middleware.ts:43` still excludes `ws/` though all WebSocket handling was stripped from `server.ts`. Harmless: no `/ws/*` route exists.

## Verification
- `git merge-base --is-ancestor eb9f9084 HEAD` → true; retirement is already live.
- Full diff read: operator routes/pages/libs deleted (`api/{send,sessions,review,threads/*,executive/*,client-report}`, `attach/[name]`, `sessions/[name]`, `operator-tools`, `attachStream`, `attachLock`, `threadConversation`, `threads`, `PortfolioCard`, `LoginBeacon`); `server.ts` reduced to a plain Next handler.
- Route allowlist (current tree): `find src/app` returns exactly the 15 files in `product-boundary-test.ts`'s `allowedRouteSources` — no extras.
- GET-only / no mutation: symphony routes export `GET` only; `symphony/page.tsx` has no form/POST/transition control; auth route is the only non-GET (POST+DELETE).
- No dangling imports to deleted modules; `server.ts` imports only `http`, `url`, `next`.
- Dependencies purged: `package.json`/`package-lock.json` no longer list `@xterm/*`, `node-pty`, `ws`, `@types/ws`.
- Retired specimen `attachLock.ts` survives only under `.prompteval/codex-task-prompt/archive/v2-product-boundary-20260719/`, digest-pinned by `golden-contract-test.py`.
- Executed read-only: `node --import tsx scripts/product-boundary-test.ts` → `OK (25 web source files audited)`, exit 0; wired into `check`→`build`.
- Smoke coverage: `smoke.ts` asserts 19 removed paths 404 and symphony POST/PATCH 404/405; `browser-smoke.ts` asserts no operator links render and removed routes 404 in Chromium.
- Middleware public paths narrowed from `startsWith` `['/login','/api/auth','/api/client-report']` to exact `includes(['/login','/api/auth'])` — strictly more restrictive.

## Verdict
DEPLOYABLE — the operator surface is fully removed with no reachable mutation path, no dangling imports, no legacy deps; enforcement is mechanical (`product-boundary-test` in `check`/`build`, ran green) plus HTTP+Chromium smoke. All three findings are cosmetic Lows.
