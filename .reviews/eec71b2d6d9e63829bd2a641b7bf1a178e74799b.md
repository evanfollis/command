# Review: eec71b2 — Assert blocked research in browser acceptance

**Date**: 2026-07-14T05-41Z  
**Reviewer**: Claude (Sonnet 4.6) — adversarial substitute (codex EROFS in tick session)  
**Change**: browser-smoke.ts (+5)

## Summary

Adds a smoke check that fires when `typedProjection` is true: asserts the dashboard exposes a `knowledge-research` signal that reports the blocked count. Five lines.

## Findings

**[info]** The assertion is gated on `typedProjection` (from the prior commit 479bd4c). This is correct: blocked research state only exists when a typed projection is present. With no projection, the check is skipped — this is a deliberate design choice, not a coverage gap.

**[info]** The assertion checks for text containing "blocked" inside the research section. It does not check the count or block codes. This is proportionate for a smoke test.

**[info]** No false-pass risk: the element is looked up with a role selector scoped to the research heading, so a generic "blocked" string elsewhere on the page won't satisfy it.

## Verdict

Safe. The assertion is bounded, gated on the right condition, and correctly reflects the d013319 design — when a typed projection is present, the research signal must surface blocked status.
