context-always-load:
  - CURRENT_STATE.md

# Command — Claude compatibility adapter

Read `AGENTS.md` as the canonical repository charter. The
`context-always-load` block remains here until the workspace SessionStart hook
has completed its provider-neutral migration (ADR-0050).
